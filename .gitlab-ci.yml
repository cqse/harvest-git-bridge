image: registry.cqse.eu/cqse/build-base

stages:
  - dist
  
dist:
  stage: dist
  script:
    - ./gradlew -b gradle/bootstrap.gradle
  artifacts:
    name: zip
    paths:
      - "*.gradle" 
      - "*.bat"
      - "*.sh"
      - "gradle"
